import sizes from "../../../sizes/sizez";

const styles = {
  backdrop: {
    position: "fixed",
    top: "0",
    left: "0",
    width: "100%",
    height: "100vh",
    background: "rgba(0, 0, 0, 0.75)",
    zIndex: "10"
  }
};

export default styles;
