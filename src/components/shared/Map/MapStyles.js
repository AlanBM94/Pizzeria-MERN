import sizes from "../../../styles/sizes/sizez";
import colors from "../../../styles/colors/colors";

const { white } = colors;

const styles = {
  map: {
    width: "100%",
    height: "100%"
  }
};

export default styles;
